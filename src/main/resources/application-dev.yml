spring:
  datasource:
    url: jdbc:mysql://localhost:3306/supercontrol_dev?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: devroot123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  data:
    redis:
      host: localhost
      port: 6379

server:
  port: 8080

logging:
  level:
    com.supercontrol.backend: DEBUG
